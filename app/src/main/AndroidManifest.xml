<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.landi.apisample"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="22"
        android:targetSdkVersion="22" />

    <application
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".MainActivity"
            android:label="@string/title_activity_main" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.MagCardActivity" android:label="@string/mag_card_sample"></activity>
        <activity android:name=".activity.ContactICCardActivity" android:label="@string/contact_ic_card_sample"></activity>
        <activity android:name=".activity.RFCardActivity" android:label="@string/rf_card_sample"></activity>
        <activity android:name=".activity.PrinterActivity" android:label="@string/printer_sample"></activity>
        <activity android:name=".activity.SerialPortActivity" android:label="@string/serial_port_sample"></activity>
        <activity android:name=".activity.CashBoxActivity" android:label="@string/cashbox_sample"></activity>
        <activity android:name=".activity.InnerScannerActivity" android:label="@string/inner_scanner_sample"></activity>
        <activity android:name=".activity.ScannerActivity" android:label="@string/scanner_sample"></activity>
        <activity android:name=".activity.ModemActivity" android:label="@string/modem_sample"></activity>
        
    </application>

</manifest>